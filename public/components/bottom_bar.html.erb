<p class="font-[HeuvelGrotesk] font-bold">that is a test</p>

<div class="flex flex-col items-center text-xs transition-all <%= active ? 'text-white font-semibold' : 'text-[var(--color-not-active-btn)]' %>">
  <%= inline_svg_tag "icons/#{icon}.svg", class: "w-6 h-6 mb-1 #{active ? 'text-white' : 'opacity-50'}" %>
  <span><%= label %></span>
</div>
